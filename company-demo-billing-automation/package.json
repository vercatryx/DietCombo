{
  "name": "company-demo-billing-automation",
  "version": "1.0.0",
  "description": "Demo copy — billing automation (Playwright + Express). Node only.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "install-browsers": "cross-env PLAYWRIGHT_BROWSERS_PATH=./playwright-browsers npx playwright install chromium",
    "restart": "node restart.js",
    "dev": "lsof -ti:3500 | xargs kill -9 2>/dev/null; node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "playwright": "^1.40.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "type": "commonjs"
}
